<?php
/**
 * FireVPS columns layout: Service Plans preset.
 *
 * Currently reuses the base columns renderer for consistency.
 */

if ( ! defined( 'ABSPATH' ) ) { exit; }

include __DIR__ . '/columns-base.php';
